# Copy this file to .env and fill in your real key. Never commit .env.
# OpenAI (https://platform.openai.com/api-keys) powers classification and the
# handlers. The deterministic guardrails run with no key.
OPENAI_API_KEY=your_openai_api_key
