# Copy this file to .env and fill in your real key. Never commit .env.
# OpenAI (https://platform.openai.com/api-keys) powers the planner and synthesiser.
OPENAI_API_KEY=your_openai_api_key

# Optional: override the model (gpt-4o-mini by default; gpt-4o also works).
# AGENT_MODEL=gpt-4o-mini
