# Copy this file to .env and fill in your real key. Never commit .env.
# Only required for --online (model drafting and revision). Reflection itself
# is deterministic and runs with no key.
OPENAI_API_KEY=your_openai_api_key
