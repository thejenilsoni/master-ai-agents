# Copy this file to .env and fill in your real key. Never commit .env.
# Only required for --online (LLM grading and answer writing). The default
# grader is deterministic and runs with no key.
OPENAI_API_KEY=your_openai_api_key
