# Copy this file to .env and fill in your real key. Never commit .env.
# OpenAI (https://platform.openai.com/api-keys) powers both the embedding model
# (for retrieval) and the LLM (for answering).
OPENAI_API_KEY=your_openai_api_key
