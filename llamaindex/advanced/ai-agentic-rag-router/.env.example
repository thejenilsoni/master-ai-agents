# Copy this file to .env and fill in your real key. Never commit .env.
# OpenAI (https://platform.openai.com/api-keys) powers the embeddings, the
# sub-question decomposition, and the answer synthesis.
OPENAI_API_KEY=your_openai_api_key
