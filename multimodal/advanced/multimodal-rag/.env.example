# Copy this file to .env and fill in your real key. Never commit .env.
# Only required for --online (vision captioning and model-written answers).
# The offline pipeline ships deterministic captions and needs no key.
OPENAI_API_KEY=your_openai_api_key
