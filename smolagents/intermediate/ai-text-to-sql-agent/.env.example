# Copy this file to .env and fill in your real key. Never commit .env.
# The Streamlit app can also take the key via its password input at runtime.
# OpenAI (https://platform.openai.com/api-keys) powers the LiteLLM model.
OPENAI_API_KEY=your_openai_api_key
