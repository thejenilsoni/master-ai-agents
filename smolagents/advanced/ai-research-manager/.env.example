# Copy this file to .env and fill in your real key. Never commit .env.
# OpenAI (https://platform.openai.com/api-keys) powers the manager and the
# managed web-search agent (via LiteLLM). Web search itself needs no key.
OPENAI_API_KEY=your_openai_api_key
