[pytest]
addopts = -q --strict-markers --disable-warnings
testpaths = tests
# The kit is designed to be copied and run in place, so tests import `chatkit`
# from the kit root rather than requiring an editable install first.
pythonpath = .
