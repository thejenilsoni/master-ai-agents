# Only needed for --online, which classifies with a model instead of the
# lexicon. Loading, scoring, verification, and --selftest are all standard
# library and run with no key.
openai
python-dotenv
