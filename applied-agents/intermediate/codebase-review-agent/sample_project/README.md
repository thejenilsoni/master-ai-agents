# Harborline Store (sample project)

A deliberately flawed miniature service used as review material for the
Codebase Review Agent. **Do not copy anything in here into real software.**
Every module contains planted defects across correctness, security and clarity
so that a review run has something real to find.

This file has an unreviewable extension, so the walker skips it — which is
itself part of what the self-test checks.
