# Copy this file to .env and fill in your real key. Never commit .env.
# OpenAI (https://platform.openai.com/api-keys) plans the analysis and writes the
# explanation. All numbers are computed locally with pandas, never by the model.
OPENAI_API_KEY=your_openai_api_key
