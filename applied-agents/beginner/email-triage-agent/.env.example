# Copy this file to .env and fill in your real key. Never commit .env.
# OpenAI (https://platform.openai.com/api-keys) classifies threads and writes drafts.
# No mail credentials are needed or accepted: this project drafts, it never sends.
OPENAI_API_KEY=your_openai_api_key
