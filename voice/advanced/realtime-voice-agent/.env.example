# Copy this file to .env and fill in your real key. Never commit .env.
# OpenAI (https://platform.openai.com/api-keys) — the realtime endpoint authenticates
# with the same key as the rest of the API.
OPENAI_API_KEY=your_openai_api_key
