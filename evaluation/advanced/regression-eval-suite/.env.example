# Copy this file to .env and fill in your real key. Never commit .env.
# Only required for --online (model-judged scoring). The default judge is
# deterministic and runs with no key, which is what lets CI run the suite free.
OPENAI_API_KEY=your_openai_api_key
