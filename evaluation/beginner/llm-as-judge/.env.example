# Copy this file to .env and fill in your real key. Never commit .env.
# OpenAI (https://platform.openai.com/api-keys) powers the live judge.
# Every offline judge (--judge keyword | first-position | verbosity |
# self-preferring) and --selftest run without this key.
OPENAI_API_KEY=your_openai_api_key
