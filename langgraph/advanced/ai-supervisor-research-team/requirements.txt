langgraph>=0.2.50,<0.3
langchain-openai>=0.2,<0.4
langchain-core>=0.3,<0.4
pydantic>=2.7,<3
python-dotenv>=1.0,<2
# Optional: enables live web search in the researcher agent.
# The project runs fine without it (researcher falls back to model knowledge).
tavily-python>=0.5,<1
