# Copy this file to .env and fill in your real key. Never commit .env.
# OpenAI (https://platform.openai.com/api-keys) powers the live chat loop.
# Not needed for --demo or --selftest, which run on the standard library alone.
OPENAI_API_KEY=your_openai_api_key
