# Copy this file to .env and fill in your real key. Never commit .env.
# OpenAI (https://platform.openai.com/api-keys) powers the chat loop and the
# model-backed summarizer. Not needed for --demo or --selftest, which use a
# deterministic summarizer and the standard library alone.
OPENAI_API_KEY=your_openai_api_key
