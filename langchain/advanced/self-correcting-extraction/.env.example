# Copy this file to .env and fill in your real key. Never commit .env.
# OpenAI (https://platform.openai.com/api-keys) powers the extraction and every
# repair attempt. Each document costs up to 3 model calls.
OPENAI_API_KEY=your_openai_api_key
